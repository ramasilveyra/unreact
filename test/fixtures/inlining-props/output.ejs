<div class="container">
  <p>This <%= name %> list is awesome</p>

  <ul>
    <% list.forEach((item) => { %>
      <li><%= item %></li>
    <% }) %>
  </ul>

  <button class="<%= `btn btn-${appearence || 'default'}` %>">Submit</button>
</div>
