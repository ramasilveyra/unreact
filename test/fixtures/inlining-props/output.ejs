<div class="container">
  <p>This <%= locals.name %> list is awesome
    <strong><%= locals.name + ' ' + "is awesome" %></strong>
  </p>

  <ul>
    <% locals.list.forEach((item) => { %>
      <li><%= item %></li>
    <% }) %>
  </ul>

  <button class="btn btn-light">Submit</button>

  <div>
    <% if (locals.error) { %>
      <p class="ulp-input-message ulp-input-errormsg">E-mail must be valid.</p>
    <% } %>
  </div>

  <div>
    <% if (locals.error) { %>
      <p class="ulp-input-message ulp-input-errormsg">E-mail must be valid.</p>
    <% } %>
  </div>

  <input type="email" value="" required />

  <input type="email" value="" />

  <input type="email" value="" />
</div>
