import react from 'react';

const Foo = () => (
  <p style={{ marginTop: '30px', flex: 1, padding: 30, left: 0, backgroundColor: null }}>
    <strong style={{ padding: null }}>Hi!</strong>
  </p>
);

export default Foo;
