<div>
  <ul>
    <% locals.list.forEach((item) => { %>
      <li><%= item %></li>
    <% }) %>
  </ul>

  <ul>
    <% locals.list.forEach((item, index) => { %>
      <li><%= item %> at <%= index %></li>
    <% }) %>
  </ul>

  <ul>
    <% locals.list.forEach((item, index, array) => { %>
      <li><%= item %> at <%= index %> of <%= array.join(', ') %></li>
    <% }) %>
  </ul>

  <ul>
    <% locals.list.filter(item => !!item).forEach((item) => { %>
      <li><%= item %></li>
    <% }) %>
  </ul>
</div>
